version: 1
disable_existing_loggers: False

formatters:
  standard:
    format: "%(asctime)s - %(levelname)s - %(name)s - %(message)s"
    datefmt: "%Y-%m-%d %H:%M:%S"

  email_formatter:
    format: |
      報警時間: %(asctime)s
      日誌等級: %(levelname)s
      發生檔案: %(filename)s (第 %(lineno)d 行)
      函數名稱: %(funcName)s

      錯誤訊息: 
      %(message)s
    datefmt: "%Y-%m-%d %H:%M:%S"

filters:
  email_filter:
    (): shared.logger.EmailFilter

handlers:
  console:
    class: logging.StreamHandler
    level: DEBUG
    formatter: standard
    stream: ext://sys.stdout

  backend_file:
    class: logging.handlers.TimedRotatingFileHandler
    level: INFO
    formatter: standard
    filename: logs/app.log
    when: D
    interval: 1
    backupCount: 30
    encoding: utf-8

  frontend_file:
    class: logging.handlers.TimedRotatingFileHandler
    level: INFO
    formatter: standard
    filename: logs/frontend.log
    when: D
    interval: 1
    backupCount: 30
    encoding: utf-8

  email:
    (): shared.logger.AsyncSMTPHandler
    level: ERROR
    formatter: email_formatter
    mailhost: ["smtp.gmail.com", 587]
    fromaddr: "sender@gmail.com"
    toaddrs: ["admin@gmail.com"]
    subject: "系統嚴重錯誤報警"
    secure: []
    filters: [email_filter]

root:
  level: WARNING
  handlers: [console]

loggers:
  app:
    level: DEBUG
    handlers: [backend_file, email]
    propagate: False

  frontend:
    level: DEBUG
    handlers: [frontend_file, email]
    propagate: False
